# 🧩 textgen-io-plugin

This plugin allows uploading and parsing of `.json` and `.yaml` files in WebUI.

## Features

- Upload `.json` or `.yaml`
- Auto parse and render as readable JSON
- Designed for model prompt integration in future

## Installation

Paste the repo URL into the "Install from URL" field in WebUI Extensions page:

